import 'package:flutter/material.dart';

class RuneColors {
  // ── Backgrounds (mais claros, azul-anil mágico) ──────────────────────────
  static const background = Color(0xFF1A1D30); // era 0xFF0A0D1A
  static const boardBg = Color(0xFF1E2238); // era 0xFF0D1022
  static const pathTile = Color(0xFF2E3B60); // era 0xFF1C2544
  static const gridLine = Color(0x338899CC); // era 0x1A — mais visível
  static const hudCard = Color(0xFF242A44); // era 0xFF111830
  static const accent = Color(0xFF9B7CF8); // roxo mais brilhante
  static const accentGlow = Color(0x559B7CF8);

  // ── Torres ───────────────────────────────────────────────────────────────
  static const towerSniper = Color(0xFF18EBFF);
  static const towerSplash = Color(0xFFFF8C00);
  static const towerFast = Color(0xFF9CFF38);
  static const towerSlow = Color(0xFFCC44FF);
  static const towerLaser = Color(0xFFFF4466);
  static const towerChain = Color(0xFFFFD54F); // raios encantados
  static const towerCore = Color(0xFFEDE7F6);

  // ── Inimigos ─────────────────────────────────────────────────────────────
  static const enemyNormal = Color(0xFFFF6B6B);
  static const enemyFast = Color(0xFFFFE040);
  static const enemyTank = Color(0xFF80FFBB);
  static const enemyFlying = Color(0xFF64D8FF);
  static const enemyRegen = Color(0xFFEE88FF);

  // ── Projéteis ────────────────────────────────────────────────────────────
  static const projSniper = Color(0xFF18EBFF);
  static const projSplash = Color(0xFFFF8C00);
  static const projFast = Color(0xFF9CFF38);
  static const projSlow = Color(0xFFDA9FEC);
  static const projLaser = Color(0xFFFF4466);
  static const projChain = Color(0xFFFFF176);
  // ── UI ───────────────────────────────────────────────────────────────────
  static const hpBar = Color(0xFF7FFFC4);
  static const hpBarLow = Color(0xFFFF6B6B);
  static const gold = Color(0xFFFFD700);

  static const mapLocked = Color(0xFF3A3A50); // era 0xFF2A2A3A
  static const mapUnlocked = Color(0xFF243060); // era 0xFF1A2240

  static const terrain = Color(0xFF131626); // era 0xFF060914
  static const terrainDot = Color(0xFF20243C); // era 0xFF131828

  static const infiniteAccent = Color(0xFFFFAB00);
  static const infiniteGlow = Color(0x55FFAB00);
}
